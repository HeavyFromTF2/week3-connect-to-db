# Todo List API - FlyRank Week 2 Assignment

A simple CRUD API built with Node.js and Express for FlyRank Week 2.

## How to Run

1. Install dependencies:
   npm install

2. Start the server:
   node index.js

The server will run on http://localhost:3000.

Just clone this repo and try it out.

## API Documentation

You can test all endpoints visually using Swagger UI:
* Swagger UI: http://localhost:3000/docs

## Available Endpoints

* GET / - API Info
* GET /health - Health Check
* GET /tasks - List all tasks
* GET /tasks/:id - Get a task by ID
* POST /tasks - Create a task
* PUT /tasks/:id - Update a task
* DELETE /tasks/:id - Delete a task

## Ai usage

I used AI as a teacher, not the all knowing god. I've used in:

- I've used ai to coach me in doing what I don't know in coding, since its the first time I've used node.js, I did not blindly ask ai to make the tasks for me, I want to try to learn with this in what I can
- Debugging and understanding errors
- Creating the Swagger UI settings file (openapi.json)
- Making this README
